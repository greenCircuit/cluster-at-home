apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name}}-svc
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    {{- range .Values.ports }}
    - protocol: {{ .protocol }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      name: {{ .name }}
    {{- end }}