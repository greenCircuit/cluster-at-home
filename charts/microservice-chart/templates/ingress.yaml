{{- if .Values.istioIngress.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Values.name }}-virtualservice
  namespace: istio-ingress
spec:
  hosts:
    - "{{ .Values.name}}.{{ .Values.istioIngress.domain }}"
  gateways:
    - {{ .Values.istioIngress.gateway }} 
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: "{{ .Values.name}}-svc.{{ .Release.Namespace }}.svc.cluster.local"
            port:
              number: {{ .Values.istioIngress.port }}
{{- end }}