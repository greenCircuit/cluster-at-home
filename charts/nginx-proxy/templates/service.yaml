apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name}}
spec:
  selector:
    name: {{ .Values.name }}
  ports:
    {{- range .Values.routes }}
    - protocol: {{ .protocol }}
      port: {{ .port }}
      targetPort: {{ .port }}
    {{- end }}